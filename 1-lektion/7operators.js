/**
 * JS OPERATOR ===
 */

const a = 1
const b = "1"

// == is used for checking equality (loosely equal)
console.log(a == b)

// === is used for strict equality (types)
console.log(a === b) // you should mostly use this one

// ALWAYS USE ===